import { Section } from '@react-email/components';

import { styles } from '@/emails/styles';

export const EmailFooter = () => {
  return (
    <Section style={styles.footer}>
      <strong>Fork it! Tickets Hunt</strong>
    </Section>
  );
};
